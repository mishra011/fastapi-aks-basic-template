version: '3.9'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fastapi-devops-app
    ports:
      - "8000:8000"
    environment:
      PYTHONPATH: /app
    restart: unless-stopped
