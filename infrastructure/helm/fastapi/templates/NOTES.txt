1. Get the application URL by running these commands:
  export SERVICE_NAME=$(kubectl get svc --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/instance={{ .Release.Name }},app.kubernetes.io/name={{ include "fastapi.name" . }}" -o jsonpath="{.items[0].metadata.name}")
  kubectl get svc --namespace {{ .Release.Namespace }} $SERVICE_NAME
